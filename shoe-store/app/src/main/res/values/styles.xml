<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TitleItem">
        <item name="android:textColor">@color/title</item>
        <item name="font">@font/roboto</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="DetailsItem">
        <item name="android:textColor">@color/details</item>
        <item name="font">@font/roboto</item>
        <item name="android:textSize">13sp</item>
        <item name="singleLine">false</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="label_style_detail">
        <item name="android:layout_marginEnd">@dimen/min_margin</item>
        <item name="android:textAlignment">textEnd</item>
    </style>

    <style name="main_button" >
        <item name="backgroundTint">@color/colorPrimaryDark</item>
        <item name="android:textColor">@color/textForPrimaryDark</item>
    </style>

    <style name="second_button">
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:textSize">18sp</item>
    </style>

</resources>
